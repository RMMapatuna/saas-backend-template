spring:
  lifecycle:
    timeout-per-shutdown-phase: 10s
  datasource:
    url: jdbc:postgresql://localhost:5432/tenant
    username: postgres
    password: 1qaz@WSX
    hikari:
      maximum-pool-size: 5
      minimum-idle: 1
      idle-timeout: 600000
      connection-timeout: 30000
      max-lifetime: 1800000
  flyway:
    enabled: true
    validate-on-migrate: true

springdoc:
  swagger-ui:
    path: /zynologic-dev/swagger-ui.html
    enabled: true
  api-docs:
    path: /zynologic-dev/api-docs
    enabled: true

